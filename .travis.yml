language: php

php:
    - "hhvm"
    - "7.0"
    - "5.6"
    - "5.5"
    - "5.4"

before_script:
   - composer self-update
   - composer install --dev --prefer-source

script: phpunit --coverage-text
